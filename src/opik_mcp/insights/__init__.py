"""Insight tools operating over the parquet write-through cache.

- ``query``   — duckdb SQL / breadcrumb resolution (query_tool)
- ``search``  — regex across cached string values (search_tool)
- ``plot``    — self-contained HTML report (plot_tool)
- ``opik_docs`` / ``read_skill`` — remote docs/skills fetch (docs_tool)
"""
