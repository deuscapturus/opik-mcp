{
  "properties": {
    "entity_type": {
      "description": "One of: experiment, project, prompt, prompt_version, test_suite, test_suite_item, thread, trace.",
      "enum": [
        "experiment",
        "project",
        "prompt",
        "prompt_version",
        "test_suite",
        "test_suite_item",
        "thread",
        "trace"
      ],
      "title": "Entity Type",
      "type": "string"
    },
    "filters": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "trace + thread: array of filter clauses applied server-side (e.g. on status, duration, or start/end time), like [{\"field\": \"status\", \"operator\": \"=\", \"value\": \"active\"}]. Accepts either a JSON array or a JSON-encoded string.",
      "title": "Filters"
    },
    "from_time": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "thread-only: ISO-8601 lower bound (inclusive) on thread created time.",
      "title": "From Time"
    },
    "name": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Optional substring filter on entity name. Supported for project, experiment, prompt, test_suite; ignored for sub-collections.",
      "title": "Name"
    },
    "page": {
      "default": 1,
      "description": "Page number (1-indexed).",
      "maximum": 10000,
      "minimum": 1,
      "title": "Page",
      "type": "integer"
    },
    "project_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Parent project UUID for project-scoped lists (trace, thread). Pass this OR project_name.",
      "title": "Project Id"
    },
    "project_name": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Parent project name \u2014 alternative to project_id for trace/thread lists, so you don't need to resolve the UUID first.",
      "title": "Project Name"
    },
    "prompt_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Required when listing prompt_versions. UUID of the prompt.",
      "title": "Prompt Id"
    },
    "search": {
      "anyOf": [
        {
          "maxLength": 200,
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "thread-only: free-text search over threads.",
      "title": "Search"
    },
    "size": {
      "default": 25,
      "description": "Items per page. Capped at 100.",
      "maximum": 100,
      "minimum": 1,
      "title": "Size",
      "type": "integer"
    },
    "sorting": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "trace + thread: array of sort clauses applied server-side, e.g. [{\"field\": \"start_time\", \"direction\": \"DESC\"}]. Accepts either a JSON array or a JSON-encoded string.",
      "title": "Sorting"
    },
    "test_suite_id": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Required when listing test_suite_items. UUID of the suite.",
      "title": "Test Suite Id"
    },
    "to_time": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "thread-only: ISO-8601 upper bound (exclusive) on thread created time.",
      "title": "To Time"
    }
  },
  "required": [
    "entity_type"
  ],
  "title": "list_entitiesArguments",
  "type": "object"
}
